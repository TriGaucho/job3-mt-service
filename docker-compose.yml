version: '3'
services:
  db:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_USER: job3Dev
      MYSQL_ROOT_PASSWORD: 12345678
      MYSQL_DATABASE: bd-dev-local
    ports:
      - "3306:3306"
    volumes:
      - /path/on/host:/var/lib/mysql