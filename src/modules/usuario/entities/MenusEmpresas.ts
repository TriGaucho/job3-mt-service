import { IMenusEmpresas } from "@shared/interfaces/menusEmpresas.interface";

export class MenusEmpresas implements IMenusEmpresas {
    titulo: string;
    icone: string;
    caminho: string;
    chave: string;
}

